Hand-written synthetic slices. No third-party dataset file may be committed
here — see PRD 04's licensing section. The real IMDb ids (four, in the
title.basics/title.ratings slices) and TMDb ids (two, `278` and `1399`, in
the movie_ids/tv_series_ids slices) are recognisable identifiers only; the
rows are typed by hand.
