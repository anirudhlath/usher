__pycache__/
*.py[cod]
.venv/
.env*
!.env.example
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
data/

# Per-worktree scratch written by the M9 execution harness
.uvicorn-port

# Usher Console (web/). `package-lock.json` is committed on purpose -- the
# Dockerfile's `npm ci` refuses to run without it, which is what makes the
# image reproducible.
web/node_modules/
web/dist/
web/dist-fixtures/
web/coverage/
web/test-results/
web/playwright-report/
web/blob-report/
web/.vite/
web/node_modules/.tmp/
